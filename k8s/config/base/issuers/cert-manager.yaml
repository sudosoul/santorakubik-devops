---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  dependsOn:
    - name: cert-manager
  acme:
    email: ${acmeEmail}
    server: ${acmeServer}
    privateKeySecretRef:
      name: letsencrypt-traefik
    solvers:
      - http01:
          ingress:
            class: traefik