apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: required-for-kustomize-but-not-used
spec:
  values:
    primary:
      nodeSelector:
        ng: data
    secondary:
      nodeSelector:
        ng: data
